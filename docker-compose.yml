version: "3.8"

services:
  bingo:
    build: .
    container_name: bingo_socketio
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    dns:
      - 8.8.8.8
      - 8.8.4.4
    restart: always
